jdk:
  - openjdk8

before_install:
  - wget https://services.gradle.org/distributions/gradle-8.0-bin.zip
  - unzip -qq gradle-8.0-bin.zip
  - export GRADLE_HOME=$PWD/gradle-8.0
  - export PATH=$GRADLE_HOME/bin:$PATH
  - gradle -v

install:
  - gradle clean build publishToMavenLocal