jdk:
  - openjdk8

before_install:
  - mkdir -p $HOME/.android
  - echo 'count=0' > $HOME/.android/repositories.cfg
  - yes | sdkmanager "platforms;android-34"

install:
  - ./gradlew -Pgroup=com.github.muhsin-k -Pversion=1.0.6 clean assembleRelease publishToMavenLocal --debug